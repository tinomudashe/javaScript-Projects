let myfunctions = (a, b) => a * b;

console.log(myfunctions(2, 3));

let hello = (value) => value;

value = 'hello';

console.log(hello(value));

add = (ingrediants) => "Add " + ingrediants;

let ingrediants = "sugar";

console.log(add(ingrediants));